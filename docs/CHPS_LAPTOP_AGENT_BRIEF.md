# CHPS Laptop Agent Brief

Shell copy — **canonical source:** `../auricrux-central/docs/CHPS_LAPTOP_AGENT_BRIEF.md`

Open the canonical file in `auricrux-central` for full task lists, prompts, and git rules.

**Quick start:** `git pull` both repos → Cursor on `fca-bid-tracker` → branch `sprint/laptop/<topic>` → PR (never push directly to `main`).
