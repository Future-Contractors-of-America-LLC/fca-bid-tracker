# 062A Next Build Targets

1. Strengthen public `Platform` and `Pricing` pages so they enumerate the real route groups already in repo.
2. Harden `Login` success and post-login guidance so users see clear entry into SaaS, LMS, and Auricrux.
3. Promote `PortalHome` and `PlatformDashboard` into stronger dispatchers for the existing real slices.
4. Add explicit Academy calls-to-action from public site and portal nav summaries.
5. Normalize route-level slice labeling so public, portal, and academy names match.
6. Add site-level truth tests that verify key public pages mention real reachable slices.
7. Add site-level truth tests that verify login/portal/academy continuity remains intact.

## Definition of done
Site-level alignment is complete only when an outside user can understand, reach, and navigate the existing SaaS and LMS slices without repo knowledge.